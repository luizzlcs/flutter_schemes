import 'package:flutter/material.dart';

class CertificatePager extends StatelessWidget {

  const CertificatePager({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Certificados'),),
           body: Container(),
       );
  }
}